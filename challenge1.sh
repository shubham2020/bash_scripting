#!/usr/bin/env bash
#this is shebang and tells the environment in which run the command

USER_NAME=$1 #no space after variable name and its value
USER_SPORT=$2

echo Hey $USER_NAME, your favourite sport is $USER_SPORT

exit 0 #exit 0 means succesfull execution
